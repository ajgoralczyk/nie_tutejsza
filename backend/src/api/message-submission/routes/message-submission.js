'use strict';

/**
 * message-submission router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::message-submission.message-submission');
